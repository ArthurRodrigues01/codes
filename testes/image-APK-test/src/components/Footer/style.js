import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    footerBox: {
        backgroundColor: 'rgb(240, 180, 75)',
        alignItems: 'center',
        justifyContent: 'center',
        padding: 30,
    }, 
    footerText: {
        fontFamily: 'Arial',
        fontWeight: 'bold',
        fontSize: 30,
        color: '#fff',
    }
})